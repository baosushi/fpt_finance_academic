@model CaptstoneProject.Areas.Teacher.Controllers.StudentEditViewModel
<div class="content">
    <div>
        <form onsubmit='return onSubmit(this)'>
            <label>Name: @Model.Name - @Model.Code</label>
            <br />
            <label>Class: @Model.Class</label>
            <br />
            <input hidden type="text" value="@Model.CourseId" id="courseId" />
            <input hidden type="text" value="@Model.Id" id="studentId" />
            @for (int i = 1; i <= Model.ComponentNames.Count; i++)
            {
                <label>@Model.ComponentNames[i - 1]</label>
                <input type="text" name="@Model.MarksComponent[i-1].CourseMarkId" value="@Model.MarksComponent[i -1].Mark" />
                <br />
                
            }
            <input type="submit" />
        </form>

    </div>
</div>
<script>
    function onSubmit(form) {
        $.ajax({
            url: '@Url.Action("GetEdit","Course")',
            data: { markList: $(form).serializeArray(), courseId: $('#courseId').val(), studentId: $('#studentId').val() },
            type: "POST",
            success: function (result) {
                swal("Edit successful!", "success");
                location.reload();
            },
            error: function (error) {
                alert("Error, please try again later.");
            }
        });
        return false;
    }
</script>